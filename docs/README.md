# Documentation Layout

This directory contains project documentation organized by purpose:

- `mqtt/` — active MQTT architecture, API, and operations docs.
- `legacy/` — historical implementation notes and migration-era docs moved from the repository root.
- `artifacts/` — generated or delivery-style text/JSON artifacts retained for reference.

The canonical project entrypoint remains the root `README.md`.
